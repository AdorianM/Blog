export interface HeroBannerType {
    title: string;
    buttonText: string;
    image: string;
}

export interface ContentMetadata {
    title: string;
    date: string;
    subtitle: string;
    slug: string;
}